import { DesativeAdicionaisDirective } from './desative.directive';

describe('DesativeDirective', () => {
  it('should create an instance', () => {
    const directive = new DesativeAdicionaisDirective();
    expect(directive).toBeTruthy();
  });
});
